version: '3.3'
services:
    web:
        #Use following image to download from Docker Hub
        #image: robertfreeman/webtester
        image: robertfreeman/coin_web
        ports:
            - 8082:8082
        networks:
            - backend

    hasher:
        image: robertfreeman/robhasher
        networks:
            - backend

    redis:
        image: robertfreeman/redis-cont
        networks:
            - backend

networks:
    backend:
